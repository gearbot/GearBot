import {h, Component} from "preact";

import {GuildPageProps} from "./props";

export default class GuildPage extends Component<GuildPageProps, {}> {
    render() {
        return (
            <div class="guildPage">
                Testing
            </div>
        )
    }
}